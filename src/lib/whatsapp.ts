// WhatsApp deep-link message builders (order + inquiry templates). See spec §6.5.
// Encode with encodeURIComponent; the delivery fee must come from the same
// cartTotals() output used in the UI so the message and cart never diverge.
//
// Implemented in Phase 3 (Pricing core) — placeholder for the Foundation phase.
export {}
