// Zustand cart store, persisted to localStorage under a versioned key.
// Items keyed by productId + packageId + hash(addons) so identical configs merge
// into box_qty increments (spec §8). Built in Phase 5 (Cart & checkout).
//
// Placeholder for the Foundation phase.
export {}
