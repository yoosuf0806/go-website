// Bake list — print-optimised route (/admin/bake-list?date=YYYY-MM-DD), opened
// in a new tab (spec §7). Built in Phase 8. Foundation-phase placeholder.
export default function BakeList() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Bake List</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Print-optimised view — Phase 8.
      </p>
    </div>
  )
}
