# Golden Oven Ops dashboard — client-side env (exposed to the browser via Vite).
# Point these at the SAME Supabase project the storefront uses, so the ops
# dashboard can read the live `orders` table and the ops_* tables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
